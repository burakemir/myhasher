load("@rules_rust//rust:defs.bzl", "rust_binary")
rust_binary(
    name = "myhasher",
    srcs = [
        "src/main.rs",
        "src/myhasher.rs",
    ],
    edition = "2021",
    deps = [
        "@crate_index//:hex",
        "@crate_index//:sha2",
        "@crate_index//:clap",
    ],
)
